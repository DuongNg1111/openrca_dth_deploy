# Core (the boilerplate runs on pure stdlib; these are for real data + EDA)
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.7
# Dev tooling
pytest>=7.4
ruff>=0.5
black>=24.0
# Optional: LLM reasoning for the process module (uncomment when needed)
# openai>=1.0
# anthropic>=0.40
